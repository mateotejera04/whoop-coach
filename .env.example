# Required: Register at developer.whoop.com
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
WHOOP_REDIRECT_URI=

# Twilio WhatsApp (sandbox credentials from twilio.com/console)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=whatsapp:+14155238886
TWILIO_TO=whatsapp:+

# Upstash Redis (auto-injected on Vercel via integration; copy from Vercel dashboard for local dev)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Vercel cron protection (auto-set by Vercel; only needed locally if testing the endpoint)
CRON_SECRET=
